# Safe local development configuration example.
# Copy to .env for local use if needed.
# Do not commit real secrets, tokens, passwords, private keys, or credentials.

APP_ENV=local
LOG_LEVEL=info
API_BASE_URL=http://localhost:8000
REQUEST_ID_HEADER=X-Request-ID
