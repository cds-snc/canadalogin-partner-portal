# Tasks

## 0. Residual Dependency Resolution

- [x] 0.1 Confirm current workspace, application-information, and workspace-scoped RP application baselines remain the shipped foundation, and limit any remaining dependency to current dashboard-summary or invitation surfaces rather than to older gap-reconciliation changes.
	Progress note (2026-08-10): frontend `/workspaces` routes and backend `/api/v1/workspaces` plus application-information and workspace-scoped RP application APIs are live in the repo, so this change no longer depends on those surfaces being restored.
- [x] 0.2 Confirm whether the default platform-admin oversight assumption is accepted or whether a dedicated oversight role is required before implementation.
	Progress note (2026-07-30): MVP2 keeps production-approval and oversight ownership with platform-admin users for now; any current superuser-only technical guard remains an implementation detail rather than a product-role decision.

## 1. Lifecycle State Model

- [x] 1.1 Confirm the record types in scope for MVP2 state tracking: workspace, application information, RP application, and any promotion-request metadata attached to environment progression.
	Progress note (2026-08-11): proposal, design, and the workspace-and-RP-application delta now consistently scope MVP2 state tracking to workspaces, application information records, and RP applications, with production promotion-request metadata attached to environment progression on workspace-scoped RP applications instead of introducing a separate fourth onboarding record type.
- [x] 1.2 Keep the lifecycle vocabulary `draft`, `submitted`, `under_review`, `approved`, and `launched`, and record the actor and rule set for each state transition, including `test`-optional entry, self-serve `test` to `staging`, allowed RP creation for `test` and `staging`, and platform-admin-reviewed `staging` to `production` progression.
	Progress note (2026-08-11): the shared vocabulary and transition posture are now recorded in `proposal.md`, `design.md`, and `specs/partner-portal-workspace-and-rp-application-management/spec.md`: new records start in `draft`, authorized submission moves them to `submitted`, platform-admin users control `under_review`, `approved`, and `launched`, `test` remains optional, `test` to `staging` stays self-serve, RP creation remains allowed for `test` and `staging`, and `staging` to `production` stays review-tracked and platform-admin approved.
- [x] 1.3 Define backend DTO, persistence, and API contract updates needed to expose onboarding state and lifecycle timestamps on list and detail surfaces, plus target-environment, promotion-status, external-reference, reviewing actor or team, and timestamp fields where progression tracking applies.
	Progress note (2026-08-11): `design.md` now includes a dedicated Slice 1 backend contract sketch grounded in the live schema and route families. It records lifecycle-field additions for `WorkspaceRead`, `ApplicationInformationRead`, `RPApplicationRead`, and current-user RP-application reads; keeps ordinary business PATCH payloads separate from governance writes; defines explicit onboarding-state transition requests plus RP-application promotion-request write models; and maps those changes onto the existing `/workspaces/**` and `/api/v1/rp-applications/mine/**` read surfaces with dedicated `/onboarding-state` and `/promotion-request` subresources for stateful writes.
- [x] 1.4 Define the Alembic migration plan and repository updates required by STD-020 and PAT-012.
	Progress note (2026-08-11): `design.md` now records a PAT-012-style expand/backfill/contract plan: an expand revision to add lifecycle columns plus a new `rp_application_promotion_request` table, a reviewable backfill step that avoids inventing missing review-trace metadata, and a contract revision that makes `onboarding_state` canonical only after verification. The same section also inventories the required model, schema, service, repository, API, and test updates under STD-020.
- [x] 1.5 Define frontend state presentation requirements for workspace, application-information, and RP-application views.
	Progress note (2026-08-11): `design.md` now records a dedicated Slice 1 frontend presentation section tied to the shipped route structure. It places full lifecycle and promotion summaries on existing workspace-admin list and detail pages, adds condensed read-only status context to `/your-applications` and current-user RP-application detail headers, keeps mutation controls off current-user partner views, and requires neutral placeholders rather than misleading approval labels when lifecycle metadata is unavailable during rollout.
- [x] 1.6 Add tests that prove state visibility and representative transition behavior.
	Progress note (2026-08-11): `design.md` now records a Slice 1 verification matrix grounded in the existing test layout. It targets backend lifecycle and promotion coverage in `backend/tests/test_workspace_service.py`, `backend/tests/test_workspaces.py`, `backend/tests/test_workspace_rp_application_registration_schema.py`, and `backend/tests/test_application_information_schema.py`, plus frontend state-visibility coverage in `frontend/tests/unit/pages/WorkspacesPage.test.tsx`, `WorkspaceDetailPage.test.tsx`, `WorkspaceApplicationsListPage.test.tsx`, `WorkspaceApplicationDetailPage.test.tsx`, `YourApplicationsPage.test.tsx`, and `YourApplicationsOAuthSetupPage.test.tsx`, including negative assertions for internal-only metadata and neutral-placeholder rollout behavior.
- [x] 1.7 Land the first backend lifecycle visibility slice on existing workspace-owned reads.
	Progress note (2026-08-11): backend models, read schemas, and `backend/src/migrations/versions/0016_onboarding_lifecycle_state_columns.py` now expose `onboarding_state` plus milestone timestamps for workspace, application information, and RP application reads. The slice keeps legacy rows nullable so rollout does not invent historical states, while targeted workspace-route tests verify missing response values serialize as `draft` on the current API contracts.
- [x] 1.8 Implement the first workspace onboarding-state transition write slice.
	Progress note (2026-08-11): `POST /workspaces/{workspace_uuid}/onboarding-state` now supports the first governed transition path on workspace records: workspace admins can move `draft` to `submitted`, platform-admin users can advance `submitted` to `under_review`, `under_review` to `approved`, and `approved` to `launched`, and invalid skips or non-admin review-state transitions fail with standard 4xx errors.
- [x] 1.9 Implement the application-information onboarding-state transition slice.
	Progress note (2026-08-11): `POST /workspaces/{workspace_uuid}/application-information/{application_information_uuid}/onboarding-state` now mirrors the workspace lifecycle write contract for application-information records, reuses the same transition and permission rules, and returns the refreshed record through the existing workspace-admin detail surface.
- [x] 1.10 Extend lifecycle transition writes to RP applications, add reviewed legacy-state backfill, and persist promotion requests.
	Progress note (2026-08-11): workspace-scoped RP-application `/onboarding-state` writes now consult persisted production promotion-request metadata instead of an unconditional production guard, dedicated `/promotion-request` GET/POST/PATCH routes persist review-tracked requests and platform-admin review outcomes on the new `rp_application_promotion_request` table, workspace-scoped RP-application reads now expose promotion-summary metadata, and migration `0017_rp_application_promotion_request_and_lifecycle_backfill.py` backfills null legacy lifecycle state to `draft` without inventing historical review timestamps, reviewers, or external references.

## 2. Application Information Readiness Indicators

- [x] 2.1 Confirm which application information sections, contacts, checklist items, and external evidence references are shown as advisory readiness inputs before submission or production progression.
	Progress note (2026-08-11): `design.md` now records the first-release readiness input set. It uses the live application-information sections (`service_name_en/fr`, `overview`, `usage`, `technology_and_protocol`, `security_and_privacy`, `migration_or_transition_plan`), active contact completeness, environment-path RP-registration presence, production-bound promotion metadata, external evidence-reference status, and external process-link availability as advisory inputs. The unresolved contact-type gate and CATS collection mechanism remain visible but advisory rather than hard in-portal blockers.
- [x] 2.2 Define the completion-indicator behavior for incomplete sections, overall readiness, and missing checklist or evidence-reference context without turning those gaps into hard in-portal gates for MVP2.
	Progress note (2026-08-11): `design.md` now defines a shared advisory readiness vocabulary (`not_started`, `incomplete`, `complete`), separate overall summaries for `submit_ready` and production-readiness attention, and explicit handling for missing checklist or evidence references as warnings rather than hard failures. The MVP2 rule remains that incomplete readiness lowers visibility and prompts action without blocking save or submission solely inside Partner Portal.
- [x] 2.3 Define PAT-017 itemized summary behavior for readiness displays and GC Design System feedback components for incomplete-state messaging.
	Progress note (2026-08-11): `design.md` now records the Slice 2 display pattern: a PAT-017 description-list summary for overall readiness facts, semantic lists for section and checklist-family summaries, and PAT-020/`GcdsNotice` warning or info notices for advisory incomplete-state messaging. The design explicitly keeps form error summaries out of read-only readiness signaling.
- [x] 2.4 Define UI and API behavior for submission or progression attempts when onboarding data, checklist items, or external evidence references are incomplete, while keeping those gaps externally gated for MVP2.
	Progress note (2026-08-11): `design.md` now defines the advisory submit/progression contract: valid saves and submissions still succeed, incomplete readiness produces warnings rather than 4xx errors, production-progression requests can be created or resubmitted while gaps remain, and the portal must not present those records as approved or launched without review-trace metadata.
- [x] 2.5 Add tests for incomplete, complete, and regression paths.
	Progress note (2026-08-11): `design.md` now records the Slice 2 verification targets for backend and frontend incomplete, complete, and regression paths, including advisory submit-success behavior, production-progression warning behavior, and protection against falsely showing approved or launched status without review trace.

## 3. Platform-Admin Oversight And Review Notes

- [x] 3.1 Record the oversight route plan: `/onboarding-oversight`, `/onboarding-oversight/queue`, and `/onboarding-oversight/reports`.
	Progress note (2026-08-11): `design.md` already records the oversight route plan in Decision 3: `/onboarding-oversight` for the authenticated overview, `/onboarding-oversight/queue` for the review backlog, and `/onboarding-oversight/reports` for aggregate reporting.
- [x] 3.2 Record the approved page pattern and shared-shell navigation path required by STD-005, STD-006, STD-017, and PAT-021.
	Progress note (2026-08-11): added `onboarding-oversight-page-pattern-decision.yaml`, which records the operational-dashboard overview pattern for `/onboarding-oversight`, separate queue and reports child routes, shared-header navigation, breadcrumb return paths, GC Design System component mapping, and evidence expectations for the oversight area.
- [x] 3.3 Define queue filters, list columns, row actions, and record-detail context for the cross-workspace review backlog, including target environment and external review reference where applicable.
	Progress note (2026-08-11): `design.md` now records the first-release queue contract for `/onboarding-oversight/queue`: lifecycle-state, record-type, department, workspace, environment, and promotion filters; a stable column set for record identity, ownership, state, progression, and external reference; simple row actions into existing detail routes; and the required record-detail context that explains why a row is in the backlog.
- [x] 3.4 Define PAT-023 table behavior for the review queue, including loading, empty, error, unauthorized, and pagination states.
	Progress note (2026-08-11): `design.md` now applies PAT-023 directly to the oversight queue: a dedicated GC-aligned data table on `/onboarding-oversight/queue`, default sort for actionable work, filter and pagination state preservation, visible missing-value placeholders, and explicit loading, empty, error, unauthorized, and partial-data behavior.
- [x] 3.5 Define review-note and checklist behavior, including platform-admin visibility and update rights, and which metadata-only surfaces remain visible to CL Admin users without exposing RP secret values.
	Progress note (2026-08-11): `design.md` now records that application-information review remains the canonical editable note/checklist surface; platform-admin users may append internal notes and update structured checklist outcomes; workspace-admin and invited-developer users do not see internal review commentary; and CL Admin visibility stays limited to metadata-only oversight, workspace, application-information, and workspace-scoped RP-application surfaces with explicit exclusion of current-user secret-management routes and all secret-value actions.
- [x] 3.6 Add backend and frontend tests for oversight filtering, review-note capture, and access control.
	Progress note (2026-08-11): `design.md` now records Slice 3 verification targets: new oversight API and service pytest modules, schema and secret-regression coverage in existing backend tests, and new or extended Vitest page coverage for the oversight overview, queue, reports, and internal application-information review panel.

## 4. Role-Boundary Guidance And Process Links

- [x] 4.1 Identify the pages or flows where workspace-member versus invited-developer guidance and onboarding documentation or process links must appear, using [openspec/specs/partner-portal-external-developer-invitations-and-scoped-access/spec.md](../../specs/partner-portal-external-developer-invitations-and-scoped-access/spec.md) for invitation-management and acceptance destinations.
	Progress note (2026-08-11): `design.md` now maps the guidance and process-link surfaces to the real route families already used by the portal: workspace application-information detail, workspace RP-application detail, current-user `/your-applications` routes, invitation acceptance at `/invitations/rp-applications/$token`, and the new oversight overview or queue routes.
- [x] 4.2 Define English and French guidance copy, documentation/process-link labels, and parity expectations under STD-017 official-languages expectations.
	Progress note (2026-08-11): `design.md` now records first-release English and French guidance titles and bodies for workspace-admin, current-user, invitation-acceptance, and CL Admin oversight contexts, along with bilingual link labels and explicit route, locale, and unilingual-destination parity expectations under STD-017.
- [x] 4.3 Add tests or review fixtures that cover the new help, link, and guidance surfaces.
	Progress note (2026-08-11): `design.md` now records Slice 4 frontend verification targets in the existing page-test files plus locale-parity checks and desktop or mobile review-fixture expectations for shared guidance components.
- [x] 4.4 Record the follow-on change boundary for partner volume-spike notification, incident reporting, and deprecation workflow so those PRD items stay visible without widening this package.
	Progress note (2026-08-11): `design.md` now keeps partner volume-spike notification, incident reporting, and deprecation workflow explicitly deferred to follow-on changes while still allowing contextual process links or guidance notices on current onboarding routes.

## 5. Aggregate Reporting

- [x] 5.1 Use the default first-release formulas unless product overrides them: invitation conversion, secret rotation hygiene, and onboarding throughput by selected period.
	Progress note (2026-08-11): `design.md` now makes the Decision 5 formulas explicit for the first release and defines the selected-period semantics, aggregate-only output boundary, and policy-window echo behavior for reporting responses.
- [x] 5.2 Define backend contract and query expectations for aggregate reporting views, including stable error codes for invalid or out-of-scope filters when clients need branching.
	Progress note (2026-08-11): `design.md` now records a dedicated `/api/v1/onboarding-oversight/reports` and `/export` read contract, shared metric and period filters, per-metric aggregate expectations, aggregate-only export rules, stable `onboarding_report_*` error codes for invalid filter paths, and a first-release authorization model where platform-admin users keep cross-workspace visibility while `RP Admin`, `RP User (Edit)`, and `Read Only` users can read only in-scope partner aggregates.
- [x] 5.3 Define frontend reporting views, filter state, and PAT-023 table behavior for authorized reporting users.
	Progress note (2026-08-11): `design.md` now defines `/onboarding-oversight/reports` as a focused reporting page that reuses the MAU-report interaction pattern for date filters, KPI summaries, and export actions while applying PAT-023 to aggregate report tables, preserving filter state in the route, and hiding out-of-scope filter affordances or internal oversight entry points for partner-side reporting readers.
- [x] 5.4 Add tests for representative aggregation, empty states, invalid-filter handling, and export behavior.
	Progress note (2026-08-11): `design.md` now records Slice 5 backend and frontend verification targets for representative report aggregates, aggregate-only CSV export, authorization, empty states, route-state preservation, locale parity, and invalid-filter recovery that keeps the last valid report visible.

## 6. Verification And Readiness

- [x] 6.1 Run `make validate-openspec-change CHANGE_ID=advance-onboarding-governance-and-reporting`.
	Progress note (2026-08-10): strict OpenSpec validation passed for `advance-onboarding-governance-and-reporting` using the local CLI workflow.
- [x] 6.2 Run targeted backend and frontend tests for state, oversight, and reporting work once implementation exists.
	Progress note (2026-08-11): complete. Focused backend pytest slices now pass for workspace/application-information lifecycle, workspace-scoped RP-application transitions and promotion-request persistence, oversight queue and reporting, and the internal application-information review-note/checklist slice in `backend/tests/test_workspace_service.py`, `backend/tests/test_workspaces.py`, `backend/tests/test_onboarding_oversight_service.py`, `backend/tests/test_onboarding_oversight_api.py`, `backend/tests/test_application_information_schema.py`, `backend/tests/test_ibm_sv_user_service.py`, `backend/tests/test_rp_application_oauth_setup.py`, and `backend/tests/test_rp_application_department_setup.py`. Focused frontend Vitest coverage also passes for current-user lifecycle rendering and fetch contracts in `frontend/tests/unit/pages/YourApplicationsPage.test.tsx`, `frontend/tests/unit/pages/YourApplicationsOAuthSetupPage.test.tsx`, and `frontend/tests/unit/features/rp-applications/rp_application-api.test.ts`, workspace-admin lifecycle visibility in `frontend/tests/unit/pages/WorkspacesPage.test.tsx`, `frontend/tests/unit/pages/WorkspaceDetailPage.test.tsx`, `frontend/tests/unit/pages/ApplicationInformationListPage.test.tsx`, `frontend/tests/unit/pages/WorkspaceApplicationsListPage.test.tsx`, Slice 2 advisory readiness and the superuser-only internal review panel in `frontend/tests/unit/pages/ApplicationInformationDetailPage.test.tsx` and `frontend/tests/unit/pages/WorkspaceApplicationDetailPage.test.tsx`, and oversight overview, queue, and reporting in `frontend/tests/unit/pages/OnboardingOversightPage.test.tsx`, `frontend/tests/unit/pages/OnboardingOversightQueuePage.test.tsx`, and `frontend/tests/unit/pages/OnboardingOversightReportsPage.test.tsx`.
- [x] 6.3 Capture standards impact for UI, accessibility, bilingual content, API, persistence, and baseline applicability before implementation handoff.
	Progress note (2026-08-11): `design.md` now includes a full STD-017-style `standards_impact` block covering UI, accessibility, official languages, security/privacy, identity/access, information management, verification, and GC web application baseline applicability for the planned onboarding slices.
- [x] 6.4 Keep PRD and OpenSpec terminology aligned if lifecycle-state, platform-admin terminology, or operational role labels change during planning.
	Progress note (2026-08-11): `proposal.md` now includes a dedicated terminology-alignment section that maps `platform-admin` to the PRD's `CL Admin` actor, preserves the PRD partner-role labels, freezes the lifecycle vocabulary, and distinguishes the user-facing production-progression concept from the tracked `promotion request` record.
